import 'package:flutter/material.dart';

final mainDrawer = Drawer(
  child: ListView(
    children: [
      DrawerHeader(
        child: Center(
          child: Text("Design by \n Waleed Alrashed"),
        ),
      )
    ],
  ),
);
